"use strict";

// Batal Tambah -------------------------------------------------------------------------------
$('#batal-tambah').on('click', function () {
    $('#form-tambah').trigger('reset');
})

// ON WINDOW LOAD -------------------------------------------------------------------------------
$(window).on("load", function () {});
